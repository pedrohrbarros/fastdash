export enum CREATEUSERPROPERTIES {
  REGISTER = 'firstname, lastname, email, password, phone',
}

export enum CREATEPRODUCTPROPERTIES {
  CREATE = 'name, price',
}

export enum CREATESELLERPROPERTIES {
  CREATE = 'name, age, location',
}

export enum CREATESALEPROPERTIES {
  CREATE = 'name, product_id, seller_id',
}
